1. сохраняем список коллизионных хешей
2. если хеш коллизионный, то, обращаемся к маленькому обычному словарю строка -> индексы
3. поэтому, на этапе компиляции, необходимо сохранить коллизионные флексии в отдельный файл вида строка - два индекса
4. а на этапе загрузки, загрузить его, объединяя значения ПО ФАКТУ (а не по хешу) совпадающих строк в одно.


1. Список флексий.

2. Делим его на словарь: хеш -> список_флексий2 с одинаковым хешем.

3. Для каждого такого списка_флексий2 с одинаковым хешем выполняем нижеследующие действия:

4. Преобразуем список_флексий2 с одним хешем в сет из строк (от каждой флексии). 

5. Если в этом сете (от списка_флексий2 с одинаковым хешем) больше одной строки, значит весь этот список_флексий2 - коллизия, 
   ибо несмотря на один хеш, присутствуют разные строки.

Если это так, то

6. удаляем все элементы списка_флексий2 из глобального списка флексий.

7. добавляем все элементы из списка_флексий2 в отдельный список для отдельной компиляции.

